<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
        http://code.alibabatech.com/schema/dubbo
        http://code.alibabatech.com/schema/dubbo/dubbo.xsd">
    <!-- 配置dubbo消费方 -->
    <!--配置消费方名称  -->
    <dubbo:application name="sport-service-course"/>
    <!--注册zookeeper 连接  192.168.200.128:2181  -->
    <dubbo:registry address="192.168.3.19:2181" protocol="zookeeper"></dubbo:registry>
    <!-- 获取服务
    id值 必须和服务方提供的id值一致 也即服务方配置ref一致，才能注入成功
     -->
    <dubbo:reference interface="priv.llf.ability.course.south.service.ICourseService"  id="courseService"/>
</beans>